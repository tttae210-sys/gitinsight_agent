# --- [GitInsight Agent 맞춤 추가 설정] ---

# 1. 로컬 벡터 DB 데이터 (ChromaDB)
# 용량이 매우 크고 각자의 환경에서 생성되어야 하므로 깃허브 업로드 방지
chroma_db/

# 2. 맥북 OS 시스템 파일
# 맥북 환경에서 폴더를 열 때마다 자동 생성되는 숨김 파일 방지
.DS_Store

# 3. VS Code 에디터 설정
# 팀원들마다 에디터 설정이 다를 수 있으므로 충돌 방지
.vscode/